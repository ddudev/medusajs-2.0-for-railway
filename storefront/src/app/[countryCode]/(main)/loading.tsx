import PageLoading from "@modules/common/components/page-loading"

/**
 * Loading state for homepage route transitions
 * Shows when navigating to the homepage
 */
export default function Loading() {
  return <PageLoading />
}
